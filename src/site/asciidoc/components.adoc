= Core Components

A class-by-class index of dbUnit's core abstractions, grouped by area. If you already
know which task you're doing, use the rest of *Using DbUnit* instead — those pages are
organized around workflows ("how do I load a CSV dataset," "how do I run an operation").
This page is for the other direction: you already have a class in hand — from JavaDoc, a
stack trace, or someone else's code — and want to know what it is and where it's covered.

Most classes below link straight to the topic page that already covers them in depth.
A few — `ITable`, `VerifyTableDefinition`, `IOperationListener`, and
`Assertion`/`DbUnitAssert` — didn't have a page anywhere else, so they get one here.

[#connections]
== Connections & Configuration

[cols="1,3", options="header"]
|===
|Class |Covered

|anchor:databaseconnection[]`link:apidocs/org/dbunit/database/IDatabaseConnection.html[IDatabaseConnection]`, `DatabaseConnection`, `DatabaseDataSourceConnection` |link:connections.html[Connections & Configuration] — wraps a JDBC connection or `DataSource`; the factory for creating datasets/tables from a live database.
|anchor:databasedataset[]`DatabaseDataSet` |link:connections.html#databasedataset[Connections & Configuration] — the `IDataSet` adapter over a live database connection.
|`CachingConnectionProvider` |link:connections.html#cachingconnectionprovider[Connections & Configuration] — caches/reuses one `IDatabaseConnection` across test methods.
|`DatabaseConfig` |link:properties.html[Properties & Features] — the feature-flag/property configuration object for a connection.
|`link:components/imetadatahandler.html[IMetadataHandler]`, `InMemoryMetadataResultSet` |*New:* link:components/imetadatahandler.html[IMetadataHandler] — corrects a vendor driver's `DatabaseMetaData` quirks; register via `DatabaseConfig.PROPERTY_METADATA_HANDLER`.
|===

[#datasets-tables]
== Datasets & Tables

[cols="1,3", options="header"]
|===
|Class |Covered

|anchor:dataset[]`link:apidocs/org/dbunit/dataset/IDataSet.html[IDataSet]`, anchor:defaultdataset[]`DefaultDataSet`, anchor:datasetbuilder[]`DataSetBuilder` |link:datasets.html[Datasets] — a named collection of `ITable`s, and the fluent builder for constructing one in code.
|`link:components/itable.html[ITable]`, `ITableMetaData`, `Column`, `Columns`, `ITableIterator` |*New:* link:components/itable.html[ITable & Table Metadata] — one table's contract, schema, and cell access.
|anchor:flatxmldataset[]`FlatXmlDataSet` |link:datasets/flatxml.html[Flat XML] — one XML element per row, the most commonly used file format.
|anchor:xmldataset[]anchor:originalxmlsample[]`XmlDataSet` |link:datasets/xml.html[XML] — the original, more verbose XML format.
|anchor:csvdataset[]`CsvDataSet` |link:datasets/csv.html[CSV] — one CSV file per table, plus a `table-ordering.txt` manifest.
|anchor:xlsdataset[]`XlsDataSet` |link:datasets/excel.html[Excel (XLS)] — one sheet per table.
|anchor:jsondataset[]`JsonDataSet` |link:datasets/json.html[JSON] — one top-level key per table, values as row-object arrays.
|anchor:yamldataset[]`YamlDataSet` |link:datasets/yaml.html[YAML] — one top-level key per table.
|`SqlLoaderControlDataSet` |link:datasets/sqlloader.html[SQL*Loader] — builds a dataset from Oracle SQL*Loader control files.
|anchor:querydataset[]`QueryDataSet` |link:datasets/query.html[Query-Based] — tables populated from arbitrary SQL queries.
|anchor:streamingdataset[]`StreamingDataSet` |link:datasets/streaming.html[Streaming Datasets] — forward-only, low-memory access over a producer.
|`DataFileLoader` and implementations |link:datasets/fileloader.html[Data File Loader] — loads a dataset file from the classpath, with `ReplacementDataSet` substitution built in.
|anchor:compositedataset[]`CompositeDataSet`, anchor:replacementdataset[]`ReplacementDataSet`, anchor:tabledecoratordataset[]`TableDecoratorDataSet`, `SortedDataSet`/`SortedTable`, `CachedDataSet` |link:datasets/decorators.html[Decorators] — wrap an existing `IDataSet`/`ITable` to add behavior.
|anchor:filtereddataset[]`FilteredDataSet` |link:datasets/decorators.html#filtereddataset[Decorators] for the `SequenceTableFilter` ordering strategy; link:filters.html[Filters] for `DatabaseSequenceFilter`, `IncludeTableFilter`, and `ExcludeTableFilter`.
|===

[#operations]
== Database Operations

[cols="1,3", options="header"]
|===
|Class |Covered

|anchor:databaseoperation[]`link:apidocs/org/dbunit/operation/DatabaseOperation.html[DatabaseOperation]` |link:operations.html[Database Operations] — abstract base for a database mutation performed before/after a test.
|anchor:insert[]`DatabaseOperation.INSERT` |link:operations.html#insert[Database Operations]
|anchor:delete[]`DatabaseOperation.DELETE` |link:operations.html#delete[Database Operations]
|anchor:deleteall[]`DatabaseOperation.DELETE_ALL` |link:operations.html#deleteall[Database Operations]
|anchor:truncate[]`DatabaseOperation.TRUNCATE_TABLE` |link:operations.html#truncate[Database Operations]
|anchor:refresh[]`DatabaseOperation.REFRESH` |link:operations.html#refresh[Database Operations]
|anchor:cleanInsert[]`DatabaseOperation.CLEAN_INSERT` |link:operations.html#cleanInsert[Database Operations]
|`DatabaseOperation.NONE`, `CompositeOperation`, `TransactionOperation`, `CloseConnectionOperation` |link:operations.html[Database Operations]
|anchor:InsertIdentityOperation[]`InsertIdentityOperation` |link:operations.html#InsertIdentityOperation[Database Operations] — MSSQL-specific, toggles `IDENTITY_INSERT`.
|===

[#comparisons-assertions]
== Data Comparisons & Assertions

[cols="1,3", options="header"]
|===
|Class |Covered

|anchor:valuecomparer[]`link:apidocs/org/dbunit/assertion/comparer/value/ValueComparer.html[ValueComparer]` |link:datacomparisons/valuecomparer.html[ValueComparer Comparison] — pluggable per-column comparison beyond plain equality.
|`FailureHandler`, `DifferenceListener`, `Difference` |link:datacomparisons/failurehandling.html[Failure Handling] — customize what happens when a comparison finds a mismatch.
|`link:components/assertion.html[Assertion]`, `DbUnitAssert`, `DbUnitValueComparerAssert` |*New:* link:components/assertion.html[Assertion & DbUnitAssert] — the static facade and the two instance-based engines it delegates to.
|===

[#filters]
== Filters

[cols="1,3", options="header"]
|===
|Class |Covered

|`ITableFilter`/`ITableFilterSimple`, `IncludeTableFilter`, `ExcludeTableFilter`, `SequenceTableFilter`, `DatabaseSequenceFilter`, `PrimaryKeyFilter` |link:filters.html[Filters] — which tables (and in what order) participate in an operation or comparison.
|`IColumnFilter`, `DefaultColumnFilter`, `GeneratedColumnFilter` |link:filters.html[Filters] — which columns participate.
|`IRowFilter`, `RowFilterTable` |link:filters.html[Filters] — which rows participate.
|===

[#datatypes]
== Data Types

[cols="1,3", options="header"]
|===
|Class |Covered

|`DataType`, `IDataTypeFactory`, `DefaultDataTypeFactory` |link:datatypes.html[Data Types] — maps SQL types to Java types; register a vendor-specific factory via `DatabaseConfig.PROPERTY_DATATYPE_FACTORY`.
|===

[#test-integration]
== Test Integration

[cols="1,3", options="header"]
|===
|Class |Covered

|`IDatabaseTester` and implementations |link:testcases/IDatabaseTester.html[IDatabaseTester Guide] — the preferred, composition-based way to wire dbUnit into a test.
|`DBTestCase` and subclasses |link:testcases.html[Test Integration] — the inheritance-based alternative.
|`PrepAndExpectedTestCase`, `PrepAndExpectedTestCaseSteps` |link:testcases/PrepAndExpectedTestCase.html[PrepAndExpectedTestCase] — turn-key prep/verify/cleanup test case.
|anchor:verifytabledefinition[]`link:components/verifytabledefinition.html[VerifyTableDefinition]` |*New:* link:components/verifytabledefinition.html[VerifyTableDefinition] — defines one table to verify: column filters and `ValueComparer`s.
|`link:components/ioperationlistener.html[IOperationListener]` |*New:* link:components/ioperationlistener.html[IOperationListener] — hooks into connection setup/teardown lifecycle events.
|`link:components/rowcountcheck.html[RowCountCheck]`, `RowCounter`, `RowCountChecker`, `RowCountSnapshot`, `RowCountDifference` |*New:* link:components/rowcountcheck.html[RowCountCheck] — opt-in diagnostic that fails a test when a table's row count moved between setup and teardown, catching prep/expected datasets that are missing a table or wrongly list one.
|===
